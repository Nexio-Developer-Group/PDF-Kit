/// Shared models for filtering/sorting used by files UI.
enum SortOption { name, modified, type }

enum TypeFilter { folder, pdf, image }

/// File type filter for functionality-specific file browsing
enum FileTypeFilter { all, pdf, images }
